


https://codeforces.com/contest/236/problem/A   ---PROBLEM LINK 









#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   string s;
   getline(cin,s);
   set<char>st;
   for(int i=0;i<s.length();i++)
   {
      st.insert(s[i]);
   }
   if(st.size()&1)
   {
     cout<<"IGNORE HIM!"<<endl;
   }
   else
   {
      cout<<"CHAT WITH HER!"<<endl;
   }
}


https://codeforces.com/contest/405/submission/124347273   ---PROBLEM LINK





#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   ll n;
   cin>>n;
   ll arr[n];
   for(int i=0;i<n;i++)
   {
     cin>>arr[i];
   }
   
   sort(arr,arr+n);
   for(auto &x:arr)
   cout<<x<<" ";
}

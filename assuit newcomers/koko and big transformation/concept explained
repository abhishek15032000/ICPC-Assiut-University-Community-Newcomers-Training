



https://codeforces.com/group/MWSDmqGsZm/contest/223206/problem/C   --- PROBLEM LINK
similar problem to that of codeforces round 732 DIV 2 A PROBLEM 



espically the sum part which didnt strike me in the contest 
but i learned that this could be done and
this problem was a direct application of it




#include<bits/stdc++.h>
using namespace std;
#define ll long long int 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll arr[100001]={0};
    ll brr[100001]={0};
    ll n,m;
    cin>>n>>m;
    ll sum1=0;
    ll sum2=0;
    for(ll i=0;i<n;i++)
    {
        cin>>arr[i];
        sum1+=arr[i];
    }
    for(ll i=0;i<m;i++)
    {
        cin>>brr[i];
        sum2+=brr[i];
    }

    if(sum1==sum2) 
    {
        cout<<"Yes"<<endl;
    }
    else
    {
        cout<<"No"<<endl;
    }
}


https://codeforces.com/group/MWSDmqGsZm/contest/223206/problem/K     -----PROBLEM LINK













#include<bits/stdc++.h>
using namespace std;
#define ll long long int
ll fib[51];
void precompute_fib()
{
    fib[0]=0;
    fib[1]=1;
    fib[2]=1;
    for(int i=3;i<51;i++)
    {
        fib[i]=fib[i-1]+fib[i-2];
    }
}

unsigned long long int expo(ll a,ll b,ll n)
{
    ll ans=1;
    while(b)
    {
        if(b&1)
        {
            ans=(ans*a)%n;
            b--;
        }
        a=(a*a)%n;
        b>>=1;
    }
    return ans;
}
bool fermats_test(ll n,ll iterations=10)
{
    if(n<=1 or n==4)
    return false;
    if(n<=3)
    return true;
    for(int i=0;i<iterations;i++)
    {
        ll a=2+rand()%(n-4);
        if(__gcd(a,n)!=1)
        return false;
        if(expo(a,n-1,n)!=1)
        return false;
    }
    return true;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    precompute_fib();
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        if(fermats_test(fib[n-1]))
        {
            cout<<"prime"<<"\n";
        }
        else
        {
            cout<<"not prime"<<"\n";
        }
    }
}

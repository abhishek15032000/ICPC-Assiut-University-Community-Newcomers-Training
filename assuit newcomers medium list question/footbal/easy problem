


https://codeforces.com/group/MWSDmqGsZm/contest/223207/problem/W  ---PROBLEM LINK
 






#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   ll n;
   cin>>n;
   cin.get();
   map<string,int>m;
   for(int i=0;i<n;i++)
   {
      string s;
      getline(cin,s);
      m[s]++;
   }
   int min=INT_MIN;
   string res;
   for(auto it=m.begin();it!=m.end();it++)
   { 
      if(it->second>min)
      {
         min=it->second;
         res=it->first;
      }
   }
   cout<<res<<endl;
}

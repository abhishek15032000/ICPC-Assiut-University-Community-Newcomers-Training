
#include <bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	char ch;
	cin>>ch;
	if(isupper(ch))
	{
		cout<<(char)tolower(ch)<<endl;
	}
	else
	{
		cout<<(char)toupper(ch)<<endl;
	}
}


#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
    char ch;
    cin>>ch;
    
    if(isdigit(ch))
     {
     	cout<<"IS DIGIT"<<endl;
     }
     else if(isalpha(ch))
     {  cout<<"ALPHA"<<endl;
     	if(isupper(ch))
     	{
     		cout<<"IS CAPITAL"<<endl;
     	}
     	else
     	{
     		cout<<"IS SMALL"<<endl;
     	}
     }
     
}

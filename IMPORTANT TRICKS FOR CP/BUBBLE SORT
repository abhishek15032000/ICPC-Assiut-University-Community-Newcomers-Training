

https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/H    ----problem link












#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	ll n;
	cin>>n;
	ll arr[n];
	for(ll i=0;i<n;i++)
	{
		cin>>arr[i];
	}
	
	// bubble sort
	
	for(ll i=0;i<n-1;i++)
	{   bool swap2=false;
		for(ll j=0;j<n-i-1;j++)
		{
			if(arr[j]>arr[j+1])
			{
				swap(arr[j],arr[j+1]);
				swap2=true;
				
			}
		}
		if(!swap2)
		{
			break;
		}
	}
	for(auto x:arr)
	{
		cout<<x<<" ";
	}
}

https://codeforces.com/group/MWSDmqGsZm/contest/223338/problem/H    ---PROBLEM LINK


CP-ALGORITHMS -REFERNCE ...OF EUCLID'S ALGORITHM FOR FINDING GCD 


CODE///


#include<bits/stdc++.h>
using namespace std;
#define ll long long int
ll gcd(ll a,ll b)
{
	while(b)
    {
    	a=a%b;
    	swap(a,b);
    }
    return a;
}

ll LCM(ll a,ll b)
{
	return a/gcd(a,b) * b;
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	ll a,b;
	cin>>a>>b;
	cout<<gcd(a,b)<<" "<<LCM(a,b)<<endl;
}
